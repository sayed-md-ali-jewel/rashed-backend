name: Build and Deploy Backend to Webuzo

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "backend/**"
      - ".github/workflows/deploy-backend.yml"

concurrency:
  group: deploy-backend-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-deploy:
    name: Build & Deploy Strapi Backend
    runs-on: ubuntu-22.04

    defaults:
      run:
        working-directory: backend

    env:
      # Strapi Environment
      HOST: ${{ vars.STRAPI_HOST || '0.0.0.0' }}
      PORT: ${{ vars.STRAPI_PORT || '1337' }}
      PUBLIC_URL: ${{ vars.STRAPI_PUBLIC_URL }}
      FRONTEND_URL: ${{ vars.FRONTEND_URL }}
      APP_KEYS: ${{ vars.APP_KEYS }}
      API_TOKEN_SALT: ${{ vars.API_TOKEN_SALT }}
      ADMIN_JWT_SECRET: ${{ vars.ADMIN_JWT_SECRET }}
      TRANSFER_TOKEN_SALT: ${{ vars.TRANSFER_TOKEN_SALT }}
      JWT_SECRET: ${{ vars.JWT_SECRET }}
      ENCRYPTION_KEY: ${{ vars.ENCRYPTION_KEY }}

      # Database Configuration
      DATABASE_HOST: ${{ vars.DATABASE_HOST }}
      DATABASE_PORT: ${{ vars.DATABASE_PORT || '5432' }}
      DATABASE_NAME: ${{ vars.DATABASE_NAME || 'doctor_portfolio' }}
      DATABASE_USERNAME: ${{ vars.DATABASE_USERNAME }}
      DATABASE_PASSWORD: ${{ vars.DATABASE_PASSWORD }}
      DATABASE_SSL: ${{ vars.DATABASE_SSL || 'false' }}
      DATABASE_SSL_REJECT_UNAUTHORIZED: ${{ vars.DATABASE_SSL_REJECT_UNAUTHORIZED || 'true' }}
      DATABASE_POOL_MIN: ${{ vars.DATABASE_POOL_MIN || '2' }}
      DATABASE_POOL_MAX: ${{ vars.DATABASE_POOL_MAX || '10' }}
      DATABASE_SCHEMA: ${{ vars.DATABASE_SCHEMA || 'public' }}

      # Webuzo FTP Connection
      FTP_SERVER: ${{ vars.BACKEND_FTP_SERVER || vars.FTP_SERVER }}
      FTP_USERNAME: ${{ vars.BACKEND_FTP_USERNAME || vars.FTP_USERNAME }}
      FTP_PASSWORD: ${{ vars.BACKEND_FTP_PASSWORD || vars.FTP_PASSWORD }}
      FTP_PROTOCOL: ${{ vars.FTP_PROTOCOL || 'ftp' }}
      FTP_PORT: ${{ vars.FTP_PORT || '21' }}
      FTP_SERVER_DIR: ${{ vars.BACKEND_FTP_SERVER_DIR || '/public_html/backend/' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Validate deploy secrets
        run: |
          missing=0
          for name in FTP_SERVER FTP_USERNAME FTP_PASSWORD APP_KEYS API_TOKEN_SALT ADMIN_JWT_SECRET TRANSFER_TOKEN_SALT JWT_SECRET ENCRYPTION_KEY DATABASE_HOST DATABASE_USERNAME DATABASE_PASSWORD; do
            if [ -z "${!name}" ]; then
              echo "::error::Missing GitHub secret: $name"
              missing=1
            fi
          done
          if [ "$FTP_PROTOCOL" != "ftp" ] && [ "$FTP_PROTOCOL" != "ftps" ]; then
            echo "::error::FTP_PROTOCOL must be either ftp or ftps. Use an SFTP action instead if your server only supports SFTP."
            missing=1
          fi
          exit "$missing"

      - name: Install dependencies
        run: npm ci

      - name: Build Strapi
        run: npm run build

      - name: Prepare deploy folder
        run: |
          rm -rf deploy
          mkdir -p deploy

          # Copy compiled output and runtime assets
          cp -r dist deploy/dist
          cp -r public deploy/public
          cp -r config deploy/config
          cp -r database deploy/database
          cp -r src deploy/src
          if [ -f favicon.png ]; then cp favicon.png deploy/favicon.png; fi

          # Copy package manifests & entry points
          cp package.json deploy/package.json
          cp package-lock.json deploy/package-lock.json
          cp server.js deploy/server.js
          cp server.js deploy/app.js

          # Install production dependencies only inside deploy folder
          cd deploy
          npm ci --omit=dev
          cd ..

          # Passenger / Webuzo restart signal
          mkdir -p deploy/tmp
          echo "restarted at $(date -u +%Y-%m-%dT%H:%M:%SZ) - ${GITHUB_SHA}" > deploy/tmp/restart.txt

          # Configure deploy package.json
          node -e "const fs=require('fs');const p='deploy/package.json';const pkg=JSON.parse(fs.readFileSync(p,'utf8'));pkg.scripts={start:'node server.js'};pkg.main='server.js';pkg.engines={node:'>=20 <=22.x.x'};fs.writeFileSync(p, JSON.stringify(pkg,null,2)+'\n');"

          # Write production environment files
          {
            echo "NODE_ENV=production"
            echo "HOST=${HOST}"
            echo "PORT=${PORT}"
            echo "PUBLIC_URL=${PUBLIC_URL}"
            echo "FRONTEND_URL=${FRONTEND_URL}"
            echo "APP_KEYS=${APP_KEYS}"
            echo "API_TOKEN_SALT=${API_TOKEN_SALT}"
            echo "ADMIN_JWT_SECRET=${ADMIN_JWT_SECRET}"
            echo "TRANSFER_TOKEN_SALT=${TRANSFER_TOKEN_SALT}"
            echo "JWT_SECRET=${JWT_SECRET}"
            echo "ENCRYPTION_KEY=${ENCRYPTION_KEY}"
            echo "DATABASE_HOST=${DATABASE_HOST}"
            echo "DATABASE_PORT=${DATABASE_PORT}"
            echo "DATABASE_NAME=${DATABASE_NAME}"
            echo "DATABASE_USERNAME=${DATABASE_USERNAME}"
            echo "DATABASE_PASSWORD=${DATABASE_PASSWORD}"
            echo "DATABASE_SSL=${DATABASE_SSL}"
            echo "DATABASE_SSL_REJECT_UNAUTHORIZED=${DATABASE_SSL_REJECT_UNAUTHORIZED}"
            echo "DATABASE_SCHEMA=${DATABASE_SCHEMA}"
            echo "DATABASE_POOL_MIN=${DATABASE_POOL_MIN}"
            echo "DATABASE_POOL_MAX=${DATABASE_POOL_MAX}"
          } > deploy/.env.production
          cp deploy/.env.production deploy/.env

      - name: Upload to Webuzo by FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ env.FTP_SERVER }}
          username: ${{ env.FTP_USERNAME }}
          password: ${{ env.FTP_PASSWORD }}
          protocol: ${{ env.FTP_PROTOCOL }}
          port: ${{ env.FTP_PORT }}
          local-dir: ./backend/deploy/
          server-dir: ${{ env.FTP_SERVER_DIR }}
          exclude: |
            **/.git*
            **/.git*/**
